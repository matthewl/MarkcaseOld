<div id="content" class="col-md-8">
  <h1>Welcome to Markcase</h1>
  <h3>A bookcase for your bookmarks</h3>          

  <hr />

  <p>Markcase is a simple bookmarking service with an emphasis on privacy. You can be as public or private as you want with your bookmarks. You can control the visibility of your account, your tag bundles and your bookmarks.</p>

  <p>Markcase lets you bookmark from the browser without any fuss. The design is simple and lightweight.</p>
</div>

<div id="sidebar" class="col-md-4">
  <% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
      <div class="alert alert-<%= name.to_s == 'notice' ? 'success' : 'danger' %>">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= content_tag :div, msg, id: "flash_#{name}" %>
      </div>
    <% end %>
  <% end %>

  <%= form_tag(home_index_path, class: '') do %>
    <%= label_tag :login, 'Login' %>
    <%= text_field_tag :login, nil, id: 'inputLogin', class: 'form-control', placeholder: 'e.g. gandalf', type: 'text' %>

    <%= label_tag :password, 'Password', class: 'mt-3' %>
    <%= password_field_tag :password, nil, id: 'inputPassword', class: 'form-control' %>

    <label class="checkbox my-3">
      <input name="remember-me" type="checkbox"> Remember me
    </label>

    <%= button_tag 'Log in', class: 'btn btn-lg btn-primary btn-block' %>
  <% end %>

  <hr/>

  <p>
    <%= link_to 'Forgot your password?', new_password_reset_path %>
  </p>

  <% if @accepting_registrations  && !Setting.first.single_account? %>
    <hr />
    <p><%= link_to 'Create your account', new_account_path %></p>
    <p>Registrations are only open for a limited time during the beta phase.</p>
  <% end %>
</div>
